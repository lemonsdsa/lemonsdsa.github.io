# Lin Mo Resume

个人简历静态网站，可直接部署到 GitHub Pages。

## 发布

1. 在 GitHub 创建一个公开仓库，例如 `lin-mo-resume`。
2. 将本目录内的文件推送到仓库的 `main` 分支。
3. 打开仓库 `Settings → Pages`，选择 `Deploy from a branch`、`main`、`/ (root)`，保存。

几分钟后访问：`https://你的用户名.github.io/lin-mo-resume/`
